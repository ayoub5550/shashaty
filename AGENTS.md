# قواعد المشروع

- بدون React وبدون أُطر عمل، وبدون تبعيات npm إن أمكن (Node القياسي فقط).
- عربي RTL أولًا.
- لا حسابات ولا قاعدة بيانات — التفضيلات في localStorage.
- مفتاح TMDB يبقى في متغير البيئة `TMDB_API_KEY` ولا يُكتب في الشيفرة أبدًا.
- الصور تمر عبر `/img` لتفادي الحجب.
